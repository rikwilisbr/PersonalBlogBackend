version: '3'
services:
  db:
    ports:
      - 5432:5432
    image: postgres
    environment:
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
      POSTGRES_USER: root
      POSTGRES_DB: personalBlog_DB
  app:
    image: personal-blog
    ports:
      - 15000:5000